# Local Agent Asset Notes

This folder contains local agent skills/config used by the project workspace.

## Rules

- Treat skill definitions as support tooling, not application runtime logic.
- Prefer editing only when the task is about agent behavior or local Codex tooling.
