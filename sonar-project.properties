sonar.projectKey=my-game-store
sonar.projectName=My Game Store
sonar.projectVersion=1.0

# Path to source directories (required)
sonar.sources=app,components,lib

# Ignore files that shouldn't be scanned (like UI components, tests, node_modules)
sonar.exclusions=**/node_modules/**,**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,.next/**,public/**

# Set the host URL to point to the local Docker container
sonar.host.url=http://localhost:9000

# Specify that we are scanning TypeScript/JavaScript
sonar.language=ts

# Authentication Token (Replace with your generated token from SonarQube)
sonar.token=squ_875339ee4dcff7578a4fff1eb559fb530b228b1d
