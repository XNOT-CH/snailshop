# Product Checkboxes API Notes

This endpoint inherits `app/api/products/AGENTS.md`.

Public read of the consent boxes for several products at once (`?ids=a,b,c`) —
the cart needs them all before checkout. Nothing here is secret; the same text
is already on the product page.
