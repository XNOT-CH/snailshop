# Admin Product Checkbox API Notes

This endpoint inherits `app/api/admin/products/AGENTS.md`.

Consent boxes for one product (list/create; `[checkboxId]` handles edit/delete).
`PRODUCT_VIEW` to read, `PRODUCT_EDIT` to write — no permission of its own.
