# Type Notes

This folder contains shared TypeScript types.

## Rules

- Put cross-feature shared types here when they do not belong to a single lib module.
- Keep feature-local types near the feature when possible.
